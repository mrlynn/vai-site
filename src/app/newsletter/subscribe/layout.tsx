export const metadata = {
  title: 'Subscribe to Vector Log',
  description:
    'Notes from building AI systems in the field. Subscribe to the Vector Log newsletter for bi-weekly AI news, practical tips, and insights from Michael Lynn.',
};

export default function NewsletterSubscribeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
